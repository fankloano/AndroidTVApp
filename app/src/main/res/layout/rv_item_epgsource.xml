<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:focusableInTouchMode="true"
    android:id="@+id/cardview_epgsource"
    android:background="@drawable/settings_item_background_dark"
    android:focusable="true"
    android:gravity="center_vertical"
    android:layout_height="52dp">

    <TextView
        android:id="@+id/tv_epgsource_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="m.ipbestuhd.com:80"
        android:textSize="14sp"
        android:maxLines="1"
        android:ellipsize="marquee"
        android:layout_marginEnd="80dp"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:textColor="?attr/textMainColor"
        android:layout_marginStart="10dp"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_epgsource_name">

        <TextView
            android:id="@+id/tv_externalOrPlaylist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="External Epg"
            android:textSize="11sp"
            android:layout_marginTop="5dp"
            android:layout_marginEnd="10dp"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:textColor="?attr/textColorLight"/>

        <TextView
            android:id="@+id/tv_lastUpdated"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            tools:text="Stalker Portal"
            android:textSize="12sp"
            android:layout_centerVertical="true"
            android:layout_marginTop="5dp"
            android:layout_marginStart="10dp"
            android:textColor="@drawable/focused_selected_textview_info"/>

        <ImageView
            android:id="@+id/iv_updatesuccess"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_toEndOf="@id/tv_lastUpdated"
            android:layout_marginStart="30dp"
            tools:src="@drawable/ic_check_green"
            android:layout_centerVertical="true"/>

    </RelativeLayout>

</RelativeLayout>