<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/relLayout_fullepgitem"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:background="@drawable/selector_tvguide_epg"
    xmlns:tools="http://schemas.android.com/tools">

    <ProgressBar
        android:id="@+id/progressBar"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:progressDrawable="@drawable/selector_tvguide_epg_progress"
        android:visibility="invisible"
        android:duplicateParentState="true"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="10dp"
        android:orientation="vertical"
        android:duplicateParentState="true">

        <TextView
            android:id="@+id/tv_Program"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Es war einmal in Amerika"
            android:textColor="@drawable/selector_textview_tvguide_title"
            android:textSize="13sp"
            android:ellipsize="end"
            android:maxLines="1"
            android:duplicateParentState="true"/>

        <TextView
            android:id="@+id/tv_SubTitleProgram"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Es war einmal in Amerika"
            android:textColor="@drawable/selector_textview_tvguide_subtitle"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:duplicateParentState="true"/>

    </LinearLayout>

    <ImageView
        android:id="@+id/iv_reminder"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/ic_alarm"
        android:visibility="invisible"
        android:layout_margin="5dp"/>
</RelativeLayout>