<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_activated="true" android:drawable="@drawable/background_border_grey_rounded" />

    <!-- Zustand: selected -->
    <item android:state_selected="true" android:drawable="@drawable/background_border_grey_up_rect" />

    <!-- Standardzustand -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/transparent" />
        </shape>
    </item>

</selector>
